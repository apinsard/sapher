define ['./sapher'], (sapher) ->
  sapher.config ['$routeProvider', ($routeProvider) ->
    # $routeProvider.when '/404',
    #   templateUrl: '/partials/404.html'
    #   controller: 'NotFoundController'
    # $routeProvider.otherwise
    #   redirectTo: '/404'
  ]
